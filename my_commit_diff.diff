commit bd6adbdd850ea03ced0ac7bf854098c700d08dd9
Author: Skottbie T. <307495077@qq.com>
Date:   Sun Aug 24 01:39:43 2025 +0800

    Teacher仪表盘优化 beta

diff --git a/client/src/pages/TeacherDashboard.jsx b/client/src/pages/TeacherDashboard.jsx
index fb11a19..ca55b1b 100644
--- a/client/src/pages/TeacherDashboard.jsx
+++ b/client/src/pages/TeacherDashboard.jsx
@@ -13,6 +13,28 @@ import useAutoRefresh from '../hooks/useAutoRefresh';
 import { getGreeting } from '../utils/greetings';
 import NicknamePrompt from '../components/NicknamePrompt';
 
+import { 
+  // 与StudentDashboard一致的图标
+  ClipboardList,    // 📋 替换 (任务描述、提交要求、活跃任务)
+  Clock,            // ⏰ 替换 (时间设置、截止时间)
+  Archive,          // 📦 替换 (归档)
+  Send,             // 📤 替换 (发布任务、恢复)
+  Plus,             // ➕ 替换 (创建班级、发布新任务)
+  BookOpen,         // 📚 替换 (我的班级、课程任务)
+  FileText,         // 📝 替换 (查看提交、课堂练习)
+  Clock3,           // 🕐 替换 (开发环境时间显示)
+  
+  // TeacherDashboard新增的图标
+  BarChart3,        // 📊 替换 (班级统计、状态信息)
+  Trash2,           // 🗑️ 替换 (删除、回收站)
+  Lock,             // 🔒 替换 (限制查看)
+  Unlock,           // 🔓 替换 (开放查看)
+  RefreshCw,        // 🔄 替换 (恢复)
+  X,                // 💀 替换 (永久删除)
+  FolderOpen,       // 📂 替换 (任务信息)
+  Pin,              // 📌 替换 (发布新任务)
+  GraduationCap     // 🏫 替换 (关联班级)
+} from 'lucide-react';
 
 const TeacherDashboard = () => {
   const [user, setUser] = useState(null);
@@ -420,7 +442,7 @@ const TeacherDashboard = () => {
   // 🚀 提前计算当前任务列表
   const currentTasks = useMemo(() => tasks[currentCategory] || [], [tasks, currentCategory]);
 
-  // 📌 移动端任务卡片渲染函数
+// 📌 移动端任务卡片渲染函数
   const renderMobileTaskCard = useCallback((task) => {
     const taskStatus = getTaskStatus(task.deadline);
     
@@ -441,7 +463,7 @@ const TeacherDashboard = () => {
         <SecondaryButton
           key="submissions"
           size="sm"
-          icon="📝"
+          icon={<FileText className="w-4 h-4" />}
           onClick={() => navigate(`/task/${task._id}/submissions`)}
           className="flex-1"
         >
@@ -454,7 +476,7 @@ const TeacherDashboard = () => {
         <PrimaryButton
           key="stats"
           size="sm"
-          icon="📊"
+          icon={<BarChart3 className="w-4 h-4" />}
           onClick={() => navigate(`/task/${task._id}/class-status`)}
           className="flex-1"
         >
@@ -468,7 +490,7 @@ const TeacherDashboard = () => {
           <SecondaryButton
             key="archive"
             size="sm"
-            icon="📦"
+            icon={<Archive className="w-4 h-4" />}
             onClick={() => setConfirmDialog({
               isOpen: true,
               title: '确认归档任务',
@@ -491,7 +513,7 @@ const TeacherDashboard = () => {
           <DangerButton
             key="delete"
             size="sm"
-            icon="🗑️"
+            icon={<Trash2 className="w-4 h-4" />}
             onClick={() => setConfirmDialog({
               isOpen: true,
               title: '确认删除任务',
@@ -514,7 +536,7 @@ const TeacherDashboard = () => {
           <SecondaryButton
             key="unarchive"
             size="sm"
-            icon="📤"
+            icon={<Send className="w-4 h-4" />}
             onClick={() => handleTaskOperation(task._id, 'unarchive')}
             disabled={batchLoading}
             className="flex-1"
@@ -527,7 +549,7 @@ const TeacherDashboard = () => {
           <SecondaryButton
             key="permission"
             size="sm"
-            icon={task.allowStudentViewWhenArchived ? '🔒' : '🔓'}
+            icon={task.allowStudentViewWhenArchived ? <Lock className="w-4 h-4" /> : <Unlock className="w-4 h-4" />}
             onClick={async () => {
               try {
                 await api.put(`/task/${task._id}/student-permission`, {
@@ -550,7 +572,7 @@ const TeacherDashboard = () => {
           <DangerButton
             key="delete"
             size="sm"
-            icon="🗑️"
+            icon={<Trash2 className="w-4 h-4" />}
             onClick={() => handleTaskOperation(task._id, 'soft_delete')}
             disabled={batchLoading}
             className="flex-1"
@@ -563,7 +585,7 @@ const TeacherDashboard = () => {
           <SecondaryButton
             key="restore"
             size="sm"
-            icon="🔄"
+            icon={<RefreshCw className="w-4 h-4" />}
             onClick={() => handleTaskOperation(task._id, 'restore')}
             disabled={batchLoading}
             className="flex-1"
@@ -576,7 +598,7 @@ const TeacherDashboard = () => {
           <DangerButton
             key="permanent"
             size="sm"
-            icon="💀"
+            icon={<X className="w-4 h-4" />}
             onClick={() => setConfirmDialog({
               isOpen: true,
               title: '确认永久删除',
@@ -631,9 +653,9 @@ const TeacherDashboard = () => {
                 ${taskStatus.status === 'urgent' 
                   ? 'bg-gradient-to-r from-red-100 to-rose-100 dark:from-red-900/30 dark:to-rose-900/30 text-red-700 dark:text-red-300 border border-red-200 dark:border-red-700/50' 
                   : taskStatus.status === 'warning'
-                  ? 'bg-gradient-to-r from-yellow-100 to-amber-100 dark:from-yellow-900/30 dark:to-amber-900/30 text-yellow-700 dark:text-yellow-300 border border-yellow-200 dark:border-yellow-700/50'
+                  ? 'bg-gradient-to-r from-orange-100 to-amber-100 dark:from-orange-900/30 dark:to-amber-900/30 text-orange-700 dark:text-orange-300 border border-orange-200 dark:border-orange-700/50'
                   : taskStatus.status === 'expired'
-                  ? 'bg-gradient-to-r from-red-100 to-rose-100 dark:from-red-900/30 dark:to-rose-900/30 text-red-700 dark:text-red-300 border border-red-200 dark:border-red-700/50'
+                  ? 'bg-gradient-to-r from-gray-100 to-slate-100 dark:from-gray-900/50 dark:to-slate-900/50 text-gray-700 dark:text-gray-300 border border-gray-200 dark:border-gray-700/50'
                   : 'bg-gradient-to-r from-green-100 to-emerald-100 dark:from-green-900/30 dark:to-emerald-900/30 text-green-700 dark:text-green-300 border border-green-200 dark:border-green-700/50'
                 }
               `}>
@@ -643,7 +665,7 @@ const TeacherDashboard = () => {
                 {task.category}
               </span>
               {currentCategory === 'deleted' && task.daysLeft !== undefined && (
-                <span className={`inline-flex items-center px-2 py-1 rounded-full text-xs font-medium ${
+                <span className={`text-xs px-2 py-1 rounded-full font-medium ${
                   task.daysLeft > 7 
                     ? 'bg-green-100 text-green-800 dark:bg-green-900/50 dark:text-green-300'
                     : task.daysLeft > 3
@@ -669,7 +691,7 @@ const TeacherDashboard = () => {
           <div className="mb-4">
             <div className="bg-gradient-to-r from-blue-50 via-indigo-50 to-purple-50 dark:from-blue-900/20 dark:via-indigo-900/20 dark:to-purple-900/20 rounded-mobile-lg p-4 border border-blue-200/50 dark:border-blue-700/30">
               <div className="flex items-start gap-2">
-                <span className="flex-shrink-0 text-blue-600 dark:text-blue-400 text-lg">📋</span>
+                <ClipboardList className="w-5 h-5 text-blue-600 dark:text-blue-400 flex-shrink-0" />
                 <p className="text-sm text-blue-800 dark:text-blue-200 line-clamp-3 leading-relaxed">
                   {task.description}
                 </p>
@@ -682,7 +704,7 @@ const TeacherDashboard = () => {
         <div className="grid grid-cols-2 gap-3 mb-4">
           <div className="bg-gray-50/80 dark:bg-gray-800/50 rounded-mobile-lg p-3 space-y-2">
             <div className="flex items-center gap-2 text-xs text-gray-600 dark:text-gray-400">
-              <span>📂</span>
+              <FolderOpen className="w-3.5 h-3.5" />
               <span className="font-medium">任务信息</span>
             </div>
             <div className="space-y-1 text-sm">
@@ -694,7 +716,7 @@ const TeacherDashboard = () => {
 
           <div className="bg-gray-50/80 dark:bg-gray-800/50 rounded-mobile-lg p-3 space-y-2">
             <div className="flex items-center gap-2 text-xs text-gray-600 dark:text-gray-400">
-              <span>⏰</span>
+              <Clock className="w-3.5 h-3.5" />
               <span className="font-medium">时间设置</span>
             </div>
             <div className="space-y-1 text-sm">
@@ -816,7 +838,8 @@ const TeacherDashboard = () => {
                 {/* 🆕 添加时间段显示（可选，用于调试） */}
                 {process.env.NODE_ENV === 'development' && (
                   <p className="text-xs text-gray-400 mb-2">
-                    🕐 {new Date().toLocaleTimeString()} - 当前时段
+                    <Clock3 className="w-3 h-3 inline mr-1" /> 
+                    {new Date().toLocaleTimeString()} - 当前时段
                   </p>
                 )}
                 
@@ -840,7 +863,7 @@ const TeacherDashboard = () => {
               <div className="flex gap-2 flex-shrink-0">
                 <PrimaryButton
                   size={isMobile ? "md" : "md"}
-                  icon="➕"
+                  icon={<Plus className="w-4 h-4" />}
                   haptic
                   onClick={() => navigate('/create-class')}
                   gradient
@@ -850,7 +873,7 @@ const TeacherDashboard = () => {
                 </PrimaryButton>
                 <SecondaryButton
                   size={isMobile ? "md" : "md"}
-                  icon="📚"
+                  icon={<BookOpen className="w-4 h-4" />}
                   haptic
                   onClick={() => navigate('/my-classes')}
                   className="flex-1 sm:flex-none"
@@ -869,8 +892,8 @@ const TeacherDashboard = () => {
             transition={{ delay: 0.1 }}
             className="bg-gradient-to-r from-white via-blue-50/30 to-purple-50/30 dark:from-gray-800 dark:via-blue-900/10 dark:to-purple-900/10 rounded-mobile-2xl border border-gray-200/60 dark:border-gray-700/60 backdrop-blur-xl shadow-mobile p-6"
           >
-            <h2 className="text-lg font-semibold text-gray-800 dark:text-gray-100 mb-4 flex items-center gap-2">
-              <span className="text-xl">📌</span>
+          <h2 className="text-lg font-semibold text-gray-800 dark:text-gray-100 mb-4 flex items-center gap-2">
+              <Pin className="w-5 h-5 text-blue-600 dark:text-blue-400" />
               发布新任务
             </h2>
 
@@ -910,8 +933,8 @@ const TeacherDashboard = () => {
                                 transition-all duration-200 min-h-[44px] px-4 py-3 text-base
                                 focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500"
                     >
-                      <option value="课堂练习">📝 课堂练习</option>
-                      <option value="课程任务">📚 课程任务</option>
+                      <option value="课堂练习">课堂练习</option>
+                      <option value="课程任务">课程任务</option>
                     </select>
                   </div>
                 </div>
@@ -938,7 +961,7 @@ const TeacherDashboard = () => {
               {/* 提交要求 */}
               <div className="bg-white/50 dark:bg-gray-800/50 rounded-mobile-xl p-4 border border-gray-200/50 dark:border-gray-700/50">
                 <h3 className="font-semibold text-gray-800 dark:text-gray-100 mb-4 flex items-center gap-2">
-                  <span>📋</span>
+                  <ClipboardList className="w-5 h-5 text-blue-600 dark:text-blue-400" />
                   提交要求
                 </h3>
                 <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
@@ -1001,11 +1024,11 @@ const TeacherDashboard = () => {
                 </div>
               </div>
 
-              {/* 关联班级 */}
+            {/* 关联班级 */}
               {myClasses.length > 0 && (
                 <div className="bg-white/50 dark:bg-gray-800/50 rounded-mobile-xl p-4 border border-gray-200/50 dark:border-gray-700/50">
                   <h3 className="font-semibold text-gray-800 dark:text-gray-100 mb-4 flex items-center gap-2">
-                    <span>🏫</span>
+                    <GraduationCap className="w-5 h-5 text-blue-600 dark:text-blue-400" />
                     关联班级
                   </h3>
                   <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
@@ -1042,7 +1065,7 @@ const TeacherDashboard = () => {
               {/* 截止时间 */}
               <div className="bg-white/50 dark:bg-gray-800/50 rounded-mobile-xl p-4 border border-gray-200/50 dark:border-gray-700/50">
                 <h3 className="font-semibold text-gray-800 dark:text-gray-100 mb-4 flex items-center gap-2">
-                  <span>⏰</span>
+                  <Clock className="w-5 h-5 text-blue-600 dark:text-blue-400" />
                   截止时间
                 </h3>
                 <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
@@ -1086,7 +1109,7 @@ const TeacherDashboard = () => {
                 type="submit"
                 size="lg"
                 fullWidth
-                icon="📤"
+                icon={<Send className="w-4 h-4" />}
                 haptic
                 gradient
                 className="font-semibold"
@@ -1116,10 +1139,10 @@ const TeacherDashboard = () => {
                 isMobile ? 'gap-1' : 'gap-1'
               }`}>
                 {[
-                  { key: 'active', label: '📋 活跃任务', count: tasks.active.length, icon: '📋', color: 'blue' },
-                  { key: 'archived', label: '📦 已归档', count: tasks.archived.length, icon: '📦', color: 'gray' },
-                  { key: 'deleted', label: '🗑️ 回收站', count: tasks.deleted.length, icon: '🗑️', color: 'red' }
-                ].map(({ key, label, count, icon, color }) => (
+                  { key: 'active', label: '活跃任务', count: tasks.active.length, icon: ClipboardList, color: 'blue' },
+                  { key: 'archived', label: '已归档', count: tasks.archived.length, icon: Archive, color: 'gray' },
+                  { key: 'deleted', label: '回收站', count: tasks.deleted.length, icon: Trash2, color: 'red' }
+                ].map(({ key, label, count, icon: IconComponent, color }) => (
                   <button
                     key={key}
                     onClick={() => handleCategoryChange(key)}
@@ -1138,7 +1161,7 @@ const TeacherDashboard = () => {
                     <div className="flex items-center justify-center gap-2">
                       {isMobile ? (
                         <div className="flex flex-col items-center gap-1">
-                          <span className="text-lg">{icon}</span>
+                          <IconComponent className="w-5 h-5" />
                           <span className="text-xs font-semibold">({count})</span>
                         </div>
                       ) : (
@@ -1161,14 +1184,17 @@ const TeacherDashboard = () => {
               {/* 批量操作按钮 */}
               {selectedTasks.size > 0 && (
                 <div className="flex gap-2">
-                  <SecondaryButton
+                <SecondaryButton
                     size="sm"
                     onClick={() => {
                       setBatchOperation(currentCategory === 'active' ? 'archive' : 
                                       currentCategory === 'archived' ? 'unarchive' : 'restore');
                       setShowBatchModal(true);
                     }}
-                    icon={currentCategory === 'active' ? '📦' : '🔄'}
+                    icon={currentCategory === 'active' ? 
+                          <Archive className="w-4 h-4" /> : 
+                          <RefreshCw className="w-4 h-4" />
+                    }
                   >
                     批量{currentCategory === 'active' ? '归档' : 
                           currentCategory === 'archived' ? '恢复' : '恢复'} ({selectedTasks.size})
@@ -1180,7 +1206,7 @@ const TeacherDashboard = () => {
                         setBatchOperation('soft_delete');
                         setShowBatchModal(true);
                       }}
-                      icon="🗑️"
+                      icon={<Trash2 className="w-4 h-4" />}
                     >
                       批量删除 ({selectedTasks.size})
                     </DangerButton>
@@ -1218,10 +1244,14 @@ const TeacherDashboard = () => {
                   exit={{ opacity: 0 }}
                   className="text-center py-12"
                 >
-                  <div className="w-20 h-20 mx-auto mb-6 bg-gradient-to-br from-gray-100 to-gray-200 dark:from-gray-700 dark:to-gray-800 rounded-full flex items-center justify-center shadow-mobile">
-                    <span className="text-gray-400 dark:text-gray-500 text-3xl">
-                      {currentCategory === 'active' ? '📋' : currentCategory === 'archived' ? '📦' : '🗑️'}
-                    </span>
+              <div className="w-20 h-20 mx-auto mb-6 bg-gradient-to-br from-gray-100 to-gray-200 dark:from-gray-700 dark:to-gray-800 rounded-full flex items-center justify-center shadow-mobile">
+                    {currentCategory === 'active' ? (
+                      <ClipboardList className="w-8 h-8 text-gray-400 dark:text-gray-500" />
+                    ) : currentCategory === 'archived' ? (
+                      <Archive className="w-8 h-8 text-gray-400 dark:text-gray-500" />
+                    ) : (
+                      <Trash2 className="w-8 h-8 text-gray-400 dark:text-gray-500" />
+                    )}
                   </div>
                   <h3 className="text-lg font-medium text-gray-800 dark:text-gray-200 mb-2">
                     {currentCategory === 'active' ? '暂无活跃任务' :
@@ -1234,7 +1264,7 @@ const TeacherDashboard = () => {
                   {currentCategory === 'active' && (
                     <PrimaryButton
                       size="md"
-                      icon="➕"
+                      icon={<Plus className="w-4 h-4" />}
                       onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}
                     >
                       发布新任务
@@ -1322,9 +1352,12 @@ const TeacherDashboard = () => {
                                 {task.description && (
                                   <div className="mb-4">
                                     <div className="bg-blue-50/60 dark:bg-blue-900/20 rounded-mobile-lg p-3 border border-blue-200/50 dark:border-blue-700/30">
-                                      <p className="text-sm text-blue-800 dark:text-blue-200 line-clamp-3">
-                                        📋 {task.description}
-                                      </p>
+                                      <div className="flex items-start gap-2">
+                                        <ClipboardList className="w-4 h-4 text-blue-600 dark:text-blue-400 flex-shrink-0 mt-0.5" />
+                                        <p className="text-sm text-blue-800 dark:text-blue-200 line-clamp-3">
+                                          {task.description}
+                                        </p>
+                                      </div>
                                     </div>
                                   </div>
                                 )}
@@ -1334,7 +1367,10 @@ const TeacherDashboard = () => {
                             {/* 任务详情网格 */}
                             <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-6">
                               <div className="bg-gray-50/80 dark:bg-gray-800/50 rounded-mobile-lg p-3">
-                                <div className="text-xs text-gray-500 dark:text-gray-400 mb-1">📂 任务信息</div>
+                                <div className="text-xs text-gray-500 dark:text-gray-400 mb-1 flex items-center gap-1">
+                                  <FolderOpen className="w-3.5 h-3.5" />
+                                  任务信息
+                                </div>
                                 <div className="space-y-1 text-sm">
                                   <p><span className="font-medium">类型:</span> {task.category}</p>
                                   <p><span className="font-medium">文件:</span> {task.needsFile ? '必交' : '可选'}</p>
@@ -1343,7 +1379,10 @@ const TeacherDashboard = () => {
                               </div>
 
                               <div className="bg-gray-50/80 dark:bg-gray-800/50 rounded-mobile-lg p-3">
-                                <div className="text-xs text-gray-500 dark:text-gray-400 mb-1">⏰ 时间设置</div>
+                                <div className="text-xs text-gray-500 dark:text-gray-400 mb-1 flex items-center gap-1">
+                                  <Clock className="w-3.5 h-3.5" />
+                                  时间设置
+                                </div>
                                 <div className="space-y-1 text-sm">
                                   <p><span className="font-medium">截止:</span> {formatDeadline(task.deadline)}</p>
                                   <p><span className="font-medium">逾期:</span> {task.allowLateSubmission ? '允许' : '不允许'}</p>
@@ -1351,7 +1390,10 @@ const TeacherDashboard = () => {
                               </div>
 
                               <div className="bg-gray-50/80 dark:bg-gray-800/50 rounded-mobile-lg p-3">
-                                <div className="text-xs text-gray-500 dark:text-gray-400 mb-1">📊 状态信息</div>
+                                <div className="text-xs text-gray-500 dark:text-gray-400 mb-1 flex items-center gap-1">
+                                  <BarChart3 className="w-3.5 h-3.5" />
+                                  状态信息
+                                </div>
                                 <div className="space-y-1 text-sm">
                                   {currentCategory === 'archived' && (
                                     <p><span className="font-medium">学生查看:</span> {task.allowStudentViewWhenArchived ? '开放' : '限制'}</p>
@@ -1361,12 +1403,12 @@ const TeacherDashboard = () => {
                               </div>
                             </div>
                             
-                            {/* 操作按钮组 */}
+                          {/* 操作按钮组 */}
                             <div className="flex flex-wrap gap-2">
                               {/* 查看提交记录按钮 */}
                               <SecondaryButton
                                 size="sm"
-                                icon="📝"
+                                icon={<FileText className="w-4 h-4" />}
                                 onClick={() => navigate(`/task/${task._id}/submissions`)}
                               >
                                 查看提交
@@ -1375,18 +1417,18 @@ const TeacherDashboard = () => {
                               {/* 班级提交情况按钮 */}
                               <PrimaryButton
                                 size="sm"
-                                icon="📊"
+                                icon={<BarChart3 className="w-4 h-4" />}
                                 onClick={() => navigate(`/task/${task._id}/class-status`)}
                               >
                                 班级统计
                               </PrimaryButton>
 
-                              {/* 根据任务状态显示不同操作按钮 */}
+                                {/* 根据任务状态显示不同操作按钮 */}
                               {currentCategory === 'active' && (
                                 <>
                                   <SecondaryButton
                                     size="sm"
-                                    icon="📦"
+                                    icon={<Archive className="w-4 h-4" />}
                                     onClick={() => setConfirmDialog({
                                       isOpen: true,
                                       title: '确认归档任务',
@@ -1402,10 +1444,10 @@ const TeacherDashboard = () => {
                                   >
                                     归档
                                   </SecondaryButton>
-
+                                  
                                   <DangerButton
                                     size="sm"
-                                    icon="🗑️"
+                                    icon={<Trash2 className="w-4 h-4" />}
                                     onClick={() => setConfirmDialog({
                                       isOpen: true,
                                       title: '确认删除任务',
@@ -1428,7 +1470,7 @@ const TeacherDashboard = () => {
                                 <>
                                   <SecondaryButton
                                     size="sm"
-                                    icon="📤"
+                                    icon={<Send className="w-4 h-4" />}
                                     onClick={() => handleTaskOperation(task._id, 'unarchive')}
                                     disabled={batchLoading}
                                   >
@@ -1437,16 +1479,16 @@ const TeacherDashboard = () => {
                                   
                                   <SecondaryButton
                                     size="sm"
-                                    icon={task.allowStudentViewWhenArchived ? '🔒' : '🔓'}
+                                    icon={task.allowStudentViewWhenArchived ? <Lock className="w-4 h-4" /> : <Unlock className="w-4 h-4" />}
                                     onClick={async () => {
                                       try {
                                         await api.put(`/task/${task._id}/student-permission`, {
                                           allowStudentViewWhenArchived: !task.allowStudentViewWhenArchived
                                         });
-                                        toast.success('✅ 权限设置成功');
+                                        toast.success(' 权限设置成功');
                                         await fetchTasks(currentCategory);
                                       } catch (err) {
-                                        toast.error(`❌ 权限设置失败：${err.response?.data?.message || err.message}`);
+                                        toast.error(` 权限设置失败：${err.response?.data?.message || err.message}`);
                                       }
                                     }}
                                     disabled={batchLoading}
@@ -1456,8 +1498,18 @@ const TeacherDashboard = () => {
                                   
                                   <DangerButton
                                     size="sm"
-                                    icon="🗑️"
-                                    onClick={() => handleTaskOperation(task._id, 'soft_delete')}
+                                    icon={<Trash2 className="w-4 h-4" />}
+                                    onClick={() => setConfirmDialog({
+                                      isOpen: true,
+                                      title: '确认删除任务',
+                                      message: `确定要删除任务"${task.title}"吗？删除后30天内可恢复。`,
+                                      onConfirm: () => {
+                                        handleTaskOperation(task._id, 'soft_delete');
+                                        setConfirmDialog(prev => ({ ...prev, isOpen: false }));
+                                      },
+                                      confirmText: '删除',
+                                      confirmVariant: 'danger'
+                                    })}
                                     disabled={batchLoading}
                                   >
                                     删除
@@ -1467,22 +1519,22 @@ const TeacherDashboard = () => {
 
                               {currentCategory === 'deleted' && (
                                 <>
-                                  <SecondaryButton
+                                  <PrimaryButton
                                     size="sm"
-                                    icon="🔄"
+                                    icon={<RefreshCw className="w-4 h-4" />}
                                     onClick={() => handleTaskOperation(task._id, 'restore')}
                                     disabled={batchLoading}
                                   >
                                     恢复
-                                  </SecondaryButton>
+                                  </PrimaryButton>
                                   
                                   <DangerButton
                                     size="sm"
-                                    icon="💀"
+                                    icon={<X className="w-4 h-4" />}
                                     onClick={() => setConfirmDialog({
                                       isOpen: true,
-                                      title: '确认永久删除',
-                                      message: `确定要永久删除任务"${task.title}"吗？此操作不可恢复！`,
+                                      title: '⚠️ 永久删除任务',
+                                      message: `确定要永久删除任务"${task.title}"吗？此操作无法撤销！`,
                                       onConfirm: () => {
                                         handleTaskOperation(task._id, 'hard_delete');
                                         setConfirmDialog(prev => ({ ...prev, isOpen: false }));
